import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pacman.app',
  appName: 'Pacman',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;